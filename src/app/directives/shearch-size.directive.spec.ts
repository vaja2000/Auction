import { ShearchSizeDirective } from './shearch-size.directive';

describe('ShearchSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShearchSizeDirective();
    expect(directive).toBeTruthy();
  });
});
